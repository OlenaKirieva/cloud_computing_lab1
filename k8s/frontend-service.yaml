apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30001 # Ми самі призначаємо порт, на якому сайт буде доступний ззовні
  type: NodePort # Дозволяє доступ з-за меж кластера